# this is a placeholder file for application properties
#number of json objects this rails server will cache at a maximum
#number of json objects this rails server will cache at a maximum
komet_name = Terminology Editor

#rest_cache_max = 1000
<%= "rest_cache_max = " + (ENV['REST_CACHE_SIZE'].nil? ? "1000" : ENV['REST_CACHE_SIZE'].to_s) %>
roles_recheck_in_seconds = 300
#if set to true standard file download applies, if false we render vhat as xml and javascript does the save (must be 'true or 'false')
vhat_export_as_file = true

#in the land of javascript this parameter tells us how many seconds between a role change warning notification
role_change_notification_time_interval = 60
#in the land of javascript this parameter tells us how many seconds between a role change checks
role_change_check_time_interval = 5

#during a Komet startup, this tells us how many threads will make isaac get requests to complete translations.
translation_pool_size = 50
