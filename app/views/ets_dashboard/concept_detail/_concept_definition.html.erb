<div class="ets-concept-definition-title">Concept</div>

<% @descriptions[:descriptions].each_with_index do |desc_hash, concept_index| %>

    <% if concept_index > 0 %>
        <br>
    <% end %>

    <div id="ets_concept_definition_panel_<%= desc_hash[:uuid] %>" class="ets-concept-definition-panel">

      <!-- main header block - always visible -->
      <div class="ets-concept-definition-panel-header">

        <!-- expand/collapse icon - controls details drawer -->
        <div class="glyphicon glyphicon-plus-sign" onclick="toggleConceptDetails('ets_concept_definition_panel_<%= desc_hash[:uuid] %>')"></div>

        <!-- description type, refset -->
        <div class="ets-concept-definition-title-block">
          <div class="ets-concept-definition-title">
            <%= desc_hash[:description_type_short] %>
          </div>
          <span class="glyphicon glyphicon-text-size"></span>
          <span><%= desc_hash[:language_short] %></span>
        </div>

        <!-- description text -->
        <div class="ets-concept-definition-text">
          <%= desc_hash[:text] %>
        </div>

      </div>

      <!-- details drawer - can be collapsed -->
      <div class="ets-concept-panel-details">

        <!-- loop through the description attributes -->
        <% desc_hash[:attributes].each_with_index do |attribute, attribute_index| %>

            <% if attribute_index > 0 %>
                <hr class="ets-concept-definition-details-hr" />
            <% end %>

          <div class="ets-concept-definition-details-row">

            <div class="ets-concept-definition-details-name">
              <%= attribute[:label] %>: <%= attribute[:text] %>
            </div>

            <div class="ets-concept-definition-details-date">
              Date: <%= attribute[:date] %>
            </div>

            <div class="ets-concept-definition-details-user">
              Author: <%= attribute[:author] %>
            </div>
          </div>

        <% end %>

      </div>

    </div>

<% end %>